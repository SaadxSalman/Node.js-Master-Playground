<!DOCTYPE html>
<html lang="en">
<head>
    <script src="https://cdn.tailwindcss.com"></script>
    <title>SaaS Dashboard</title>
</head>
<body class="bg-gray-50 min-h-screen">
    <nav class="bg-indigo-600 p-4 text-white shadow-lg">
        <div class="container mx-auto flex justify-between">
            <h1 class="text-xl font-bold uppercase tracking-widest"><%= tenant.name %></h1>
            <span class="bg-indigo-500 px-3 py-1 rounded text-sm">Tenant ID: <%= tenant.id %></span>
        </div>
    </nav>

    <main class="container mx-auto mt-10 p-6">
        <div class="bg-white rounded-xl shadow-md p-8">
            <h2 class="text-2xl font-semibold mb-6 text-gray-800">Company Projects</h2>
            
            <div class="grid gap-4">
                <% projects.forEach(project => { %>
                    <div class="border-l-4 border-indigo-500 bg-gray-50 p-4 flex justify-between items-center rounded-r-md">
                        <span class="text-lg font-medium text-gray-700"><%= project.title %></span>
                        <span class="text-xs text-gray-400">Project #<%= project.id %></span>
                    </div>
                <% }) %>
            </div>
        </div>

        <div class="mt-8 text-center text-sm text-gray-500">
            <p>Try changing the URL query to <code>?tenant=t_2</code> to see data isolation in action.</p>
        </div>
    </main>
</body>
</html>
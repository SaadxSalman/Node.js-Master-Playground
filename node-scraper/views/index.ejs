<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node Scraper | saadxsalman</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen">

    <div class="container mx-auto px-4 py-10">
        <header class="text-center mb-12">
            <h1 class="text-4xl font-extrabold text-blue-400 mb-2">Live Web Scraper</h1>
            <p class="text-gray-400 italic">Extracting headlines from: <span class="text-green-400">BBC Technology</span></p>
        </header>

        <% if (locals.error) { %>
            <div class="bg-red-500/20 border border-red-500 text-red-200 p-4 rounded-lg mb-6">
                <%= error %>
            </div>
        <% } %>

        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-1 max-w-4xl mx-auto">
            <% articles.forEach((article, index) => { %>
                <div class="bg-gray-800 border border-gray-700 p-6 rounded-xl hover:border-blue-500 transition-all group shadow-lg">
                    <div class="flex items-start justify-between">
                        <div class="flex-1">
                            <span class="text-xs font-bold text-blue-500 uppercase tracking-widest">Headline #<%= index + 1 %></span>
                            <h2 class="text-xl font-semibold mt-1 group-hover:text-blue-300 transition-colors">
                                <%= article.title %>
                            </h2>
                        </div>
                        <a href="<%= article.link %>" target="_blank" class="ml-4 bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition-all">
                            Read More
                        </a>
                    </div>
                </div>
            <% }); %>
        </div>

        <footer class="mt-16 text-center text-gray-500 text-sm">
            Built by <strong>saadxsalman</strong> using Node.js, Axios, and Cheerio.
        </footer>
    </div>

</body>
</html>
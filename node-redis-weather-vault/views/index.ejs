<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Cache</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-900 text-white min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full p-8 bg-slate-800 rounded-2xl shadow-2xl border border-slate-700">
        <h1 class="text-3xl font-bold mb-6 text-center text-blue-400">Weather API</h1>
        
        <form action="/" method="GET" class="mb-8">
            <div class="flex gap-2">
                <input type="text" name="city" placeholder="Enter city..." 
                    class="flex-1 px-4 py-2 rounded-lg bg-slate-700 border border-slate-600 focus:outline-none focus:border-blue-500">
                <button type="submit" class="bg-blue-600 hover:bg-blue-700 px-6 py-2 rounded-lg font-semibold transition">Search</button>
            </div>
        </form>

        <% if (locals.data) { %>
            <div class="text-center">
                <h2 class="text-4xl font-semibold mb-2"><%= data.name %>, <%= data.sys.country %></h2>
                <div class="text-6xl mb-4">
                    <%= Math.round(data.main.temp) %>°C
                </div>
                <p class="text-slate-400 capitalize text-lg mb-6"><%= data.weather[0].description %></p>
                
                <div class="inline-block px-4 py-1 rounded-full text-xs font-mono border border-emerald-500/50 bg-emerald-500/10 text-emerald-400">
                    Source: <%= source %>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>